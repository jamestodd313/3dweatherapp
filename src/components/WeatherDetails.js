import React from 'react'

export const WeatherDetails = (data) => {
    // const {weatherData, locationData} = dat
    console.log(data.data.locationData)
    return (
        <h1>hello</h1>
    )
}
